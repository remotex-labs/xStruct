{
  "name": "constract",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "scripts": {
    "start": "node -r ts-node/register/transpile-only -r tsconfig-paths/register dist/index.js",
    "dev": "tsnd  --rs false --no-notify --respawn -r tsconfig-paths/register src/index.ts",
    "build": "tsc -p ./tsconfig.release.json",
    "lint": "eslint -c eslint.json 'app/**/*.ts'",
    "test": "jest --detectOpenHandles",
    "test:cover": "jest --coverage",
    "test:watch": "jest --watchAll --detectOpenHandles"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^29.5.11",
    "eslint": "^8.56.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "ts-node-dev": "^2.0.0",
    "tsconfig-paths": "^4.2.0",
    "tsconfig-paths-jest": "^0.0.1",
    "typescript": "^5.3.3"
  }
}
